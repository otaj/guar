# guar

Android app for Beancount ledgers (`dev.otaj.guar`).

This package is the Flutter UI. Parse, book, and query live in `guar_parser`,
`guar_book`, and `guar_query` — do not invent those semantics here.
