# ANOMALY: Lima resolves includes within the core parser
# and we already have a test for including a file which exists,
# so this is the failing case
# guar: same decision (missing include target is an error)
errors {
  message: "include failed"
}
