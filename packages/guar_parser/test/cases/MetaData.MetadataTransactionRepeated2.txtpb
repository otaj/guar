# ANOMALY: duplicated metadata keys is a parse error for Lima
# guar: same decision (duplicate metadata keys are parse errors)
errors {
  message: "duplicate key test"
}
errors {
  message: "duplicate key test"
}
errors {
  message: "duplicate key test"
}
