name: guar_plugins
description: Stock Beancount and reds booking plugins for guar.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  decimal: ^3.2.4
  guar_domain:
    path: ../guar_domain

dev_dependencies:
  guar_book:
    path: ../guar_book
  guar_parser:
    path: ../guar_parser
  lints: ^6.0.0
  test: ^1.26.3
